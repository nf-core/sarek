// Minimal config file for tests
process {
    // Fudge the naming to make sure we don't have file name clashes in tests
    withName: 'POST_VARIANTCALLING:CONCATENATE_GERMLINE_VCFS:ADD_INFO_TO_VCF' {
        ext.prefix = { "${meta.id}.${meta.num}"}
    }
}
