/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running SnpSift annotation tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to test SnpSift annotation.

    Use as follows:
        nextflow run nf-core/sarek -profile annotation,annotation_snpsift,docker --outdir <OUTDIR>
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

params {
    input = "${projectDir}/tests/csv/3.0/vcf_single.csv"
    step  = 'annotate'
    tools = 'snpsift'

    // Use minimal GRCh37 database set (dbSNP only)
    snpsift_db_set = 'GRCh37_minimal'

    // For testing, could also use a custom minimal database
    // snpsift_dbs = "${projectDir}/tests/test_data/snpsift_test_dbs.json"
}
