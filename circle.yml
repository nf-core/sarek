machine:
  java:
    version: openjdk8

  services:
    - docker

  environment:
    NXF_VER: 0.23.0

dependencies:
  override:
  - curl -fsSL get.nextflow.io | bash

test:
  post:
    - ./test/test_docker.sh
